# Repo-only files (must not be applied into $HOME)
README.md
WINDOWS.md
WSL.md
bootstrap.sh
bootstrap-windows.ps1
bootstrap-wsl.sh

# Logs / runtime cruft that lives next to configs
.config/aerospace/*.log
.glzr/**/*.log
.glzr/**/*.bak
*.bak
.DS_Store
.config/raycast/extensions
.config/raycast/ai

{{ if not (or (eq .chezmoi.os "darwin") (eq .chezmoi.os "linux")) }}
# POSIX shell config
.zprofile
.zshrc
{{ end }}

{{ if ne .chezmoi.os "linux" }}
# Linux/WSL-only shell and package config
.bashrc
.profile
.config/wsl/**
{{ end }}

{{ if ne .chezmoi.os "darwin" }}
# macOS-only config
.config/aerospace/**
.config/homebrew/**
.config/kitty/**
.config/spotify-player/**
Library/**
{{ end }}

{{ if and (ne .chezmoi.os "darwin") (ne .chezmoi.os "linux") }}
# tmux config applies on macOS and Linux/WSL only
.config/tmux/**
{{ end }}

{{ if ne .chezmoi.os "windows" }}
# Windows-only config
Documents/**
.glzr/**
.config/powershell/**
.config/scoop/**
.config/winget/**
run_once_after_10-install-windows-packages.ps1.tmpl
{{ end }}
